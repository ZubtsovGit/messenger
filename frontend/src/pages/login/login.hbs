<div class="container">
    <Form class="login-form">
        <h2 class="login__label">{{test}}</h2>
        {{> Input label="Login" }}
        {{> Input label="Password" }}
        {{> Button label="Sign in" type="primary" }}
        {{> Button label="Sign up" type="link" }}
    </Form>
</div>